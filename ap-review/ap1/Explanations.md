A — need divisor check → inputVal % k == 0
A — pattern builds like:
3 →    *
2 →   **
1 →  ***
B — object is type B → overridden method → calls B.show()
C — 0 > negatives → fails threshold condition
A — && x forces x = true → otherwise whole thing false
A — "art" split across words → breaks logic → "rattrapsimilartoday"
C — outer increments by 1 → inner repeats → lines printed twice pattern
B — must be 1 * 1
E — 2 dice → 1 + (int)(Math.random()*6) twice
D — no relationship (student ≠ athlete)
E — reverse string == original → "noon" → true
B — (x&&y)&&!(x||y) → impossible → always false
A — k = 1,4,7 → +3 each:
{17,20,21,42,45,69,48,51,39}
C — if a == b skip both → returns c (can be smaller)
A — recursion stops at 10 → prints once → "10"
E — tracks max column index → returns y
D — adds odd numbers: 1 + 3 + 5 + ...
D — int math:
404/10 = 40 → 40*10 = 400 → +1 = 401
E — x always odd → x % 2 != 0 → nothing prints
E — counts freq after index → returns most frequent index
D — recursion trace:
WATC → WAT → WA → W → base
A — just iterate nested list → simplest
C — partially sorted (3,4) not full
C — III true, II false → only choice left
C — sum: 4 + 3 + 2 + 1
A — new int[5] → all 0s, val = 6, string shortened
B — starts from end → finds 4 < 6
B — compare 1 vs 5 → take larger → 5
A — counts decimal steps
B — makes pricing logic correct